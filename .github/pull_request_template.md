## Summary

Describe the change and the ALT contract it affects.

## Checks

- [ ] `uv run ruff check .`
- [ ] `uv run mypy src`
- [ ] `uv run pytest`
- [ ] `uv run pip-audit`
- [ ] `uv run altk audit-public --strict`

## Theory Alignment

State whether this implements, approximates, defers, or explicitly does not
certify the relevant paper claim.
